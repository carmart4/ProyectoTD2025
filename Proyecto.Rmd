---
title: "Análisis de Tickets de Supermercado: Mini Proyecto 2025"
author: |
  <center>
  Ferrer Font, Neus<br>
  Navarro García, Marta<br>
  Rodríguez Simón, Alba<br>
  Aissa Djellouli, Mohammed<br>
  Balbastre Aparisi, Héctor<br>
  Ruiz Ripoll, Joan<br>
  Martinez Torres, Carlos Santiago
  </center>
date: "`r Sys.Date()`"
output:
  html_document:
    echo: true
    number_sections: true
    theme: lumen
    toc: true
  bookdown::pdf_document2:
    toc: true
    toc_depth: 3
    number_sections: true
    figure_caption: "Figura" # Referencias en castellano
    table_caption: "Tabla"
  prettydoc::html_pretty:
    theme: cayman
    highlight: github
  html_notebook:
    echo: true
    number_sections: true
    toc: true
  pdf_document: default
  bookdown::html_document2:
    echo: true
    number_sections: true
    theme: spacelab
    toc: true
    figure_caption: "Figura"
    table_caption: "Tabla"
always_allow_html: true
params:
  lang: ES
lang: "`r switch(params$lang, ES = 'es-ES', EN = 'en-US')`"
subtitle: "Tratamiento de datos. Grado en Ciencia de Datos - UV"
language:
  label:
    fig: 'Figura '
    tab: 'Tabla '
    eq: 'Ecuación '
    thm: 'Teorema '
    lem: 'Lema '
    def: 'Definición '
    cor: 'Corolario '
    prp: 'Proposición '
    exm: 'Ejemplo '
    exr: 'Ejercicio '
    proof: 'Demostración. '
    remark: 'Nota: '
    solution: 'Solución. '
---

# Importacion de librerias 

Limpiamos el entorno
```{r}
rm(list = ls())

```

Creamos un vector con los paquetes que vamos a utilizar 
```{r}
paquetes <- c("pdftools", "dplyr", "stringr", "dplyr", "ggplot2", "tidyverse")
```

Implementamos una funcion que nos permite cargar las librerias que no están disponibles hasta el momento

## Funcion para instalar y cargar librerias nuevas
```{r}
instalar_y_cargar <- function(paquetes) {
  
  nuevos <- setdiff(paquetes, rownames(installed.packages()))
  
  if (length(nuevos)) { install.packages(nuevos) }

  invisible(lapply(paquetes, library, character.only = TRUE))
}

```

Cargamos

```{r}
instalar_y_cargar(paquetes)
```


# Como vamos a almacenar la informacion? Que informacion queremos almacenar? 

```{r}
# Dataframe que contendrá el nombre del fichero leido y la factura a la que pertenece. 

FicheroLeido <- data.frame( FacturaFichero = character(),
                        NombreFichero = character(),
                        stringsAsFactors = FALSE
                        )

# En caso de no ser leido, porque no pertenece a Mercadona, guardaremos cual fichero ha sido 
# y lo almacenaremos en otro df, para identificarlo posteriormente.

NoLeido <- data.frame( NombreFichero = character(),
                        stringsAsFactors = FALSE
                        )


# Dataframe que contendrá la información de las diferentes tiendas de las facturas.  

Tienda <- data.frame( IdTienda = character(),
                      Direccion = character(),
                      CodigoPostal = character(),
                      Municipio = character(),
                      Telefono = character(),
                      stringsAsFactors = FALSE
) 

# Dataframe que contendra la informacion de las compras realizadas. 

Compra <- data.frame( Fecha = character(),
                      IdTienda = character(),
                      IdFactura = character(),
                      Producto = character(),
                      Cantidad = character(),
                      PrecioUnitario = character(),
                      Importe = character(),
                      TipoProducto = character(),
                      Parking = logical(), 
                      stringsAsFactors = FALSE
) 

```

# Funcion EsMercadona, que comprueba si la factura es de Mercadona o no

```{r}
EsMercadona <- function(NombrePDF){
  
  # Recibe el nombre el fichero en formato pdf
  
  carpeta <- 'data/'
  fichero <- paste0(carpeta, NombrePDF)
  
  texto <- pdf_text(fichero) # Extrae el texto del pdf.
  
  #texto <- iconv(texto, from = "latin1", to = "UTF-8") # Corregimos el error para valores extraños.

  lineas <- strsplit(texto, "\n")[[1]] # Divide el texto en diferentes lineas, por cada salto de linea.
  lineas <- trimws(lineas) # Elimina espacios en blanco al inicio y final de cada linea.
  
  # Para verificar que es de Mercadona y que se ha leido correctamente la factura, vamos a evaluar la primera linea.
  
  Mercadona <- grepl('MERCADONA', lineas[1])  # Valor bool para saber si es de Mercadona. 
  CIF <- grepl('A-46103834', lineas[1]) # Valor bool para saber si reconoce el CIF de Mercadona.
  
  if ((!Mercadona) | (!CIF)){
    Mercadona <- FALSE # Esto es preventivo, para el caso del ticket3.pdf
    lineas <- list()
  } 
  
  # Mercadona = Valor bool
  # lineas = lista de las lineas leidas; sera vacia en caso de no ser de Mercadona
  
  informacion <- list(Mercadona, lineas)
  
  return(informacion)
}
```

# Funcion que lee la informacion de la tienda y de los productos por factura

```{r}
# lineas 'recuperadas' de la funcion EsMercadona
ExtraeInformacion <- function(Lineas) {
  
    # Obtenemos el rango de filas que contienen el encabezado, fijo

    info_compra <- grep(x=Lineas, pattern='MERCADONA')[1]
    info_compra_final <- grep(x=Lineas, pattern='FACTURA')[1]
  
    # Definimos el encabezado y extraemos informacion 
    
    encabezado <- Lineas[info_compra:info_compra_final]
    
    direccion <- encabezado[2]

    # Ubicacion (CP y municipio)
    ubicacion <- strsplit(encabezado[3], split = ' ')[[1]] 
    
    CP <- ubicacion[1]
    municipio <- ubicacion[2]
    
    telefono <- strsplit(encabezado[4], split = ': ')[[1]][2]
    
    # Tiempo (fecha y hora)
    tiempo <- strsplit(encabezado[5], split = ' ')[[1]][1:2]
    
    fecha <- tiempo[1] # String de la fecha en formato dd/mm/aaaa
    hora <- tiempo[2] # 00:00
    
    # Factura (tienda, caja, idfactura)
    factura <- strsplit(encabezado[6], split = ": ")[[1]]
    factura <- strsplit(factura[2], split = "-")[[1]]
    
    id_tienda <- factura[1]
    n_caja <- factura[2]
    id_factura <- factura[3]
    
    # Esta es la informacion del encabezado. Fija para cada factura 
    InformacionCompra <- list(direccion, CP, municipio, telefono, fecha, hora, id_tienda, n_caja, id_factura)
    
    # Obtenemos el rango de filas que contienen los productos
    
    indice_inicio_productos <- grep(x=Lineas, pattern='Descripción')[1]
    indice_final_productos <- grep(x=Lineas, pattern='TOTAL')[1]
    
    Productos <- Lineas[(indice_inicio_productos+1):(indice_final_productos-1)]
    Productos <- str_replace_all(Productos, "\\s+", " ") # Eliminamos los espacios multiples
    
    
    # InformacionCompra = lista con la informacion del encabezado
    # Productos = lista con los productos que contiene la factura
    valores <- list(InformacionCompra, Productos)
    
    return(valores)
}
```


# Funcion que detecta si hay parking o no. Filtro 1. 

```{r}
# Como parking está al final del todo, es el filtro que aplicamos primero.
# ListaProductos = lista 'recuperada' de ExtraerInformacion
DetectaParking <- function(ListaProductos){
  
  Parking <- FALSE
  
  if (any(grepl(x=ListaProductos, pattern='PARKING'))) {
    Parking <- TRUE
    indice_parking <- grep(x=ListaProductos, pattern='PARKING')
    ListaProductos <- ListaProductos[1:(indice_parking-1)]
  }
  
  # Parking = Valor bool para saber si tiene parking o no

  valores <- list(Parking, ListaProductos)
  return(valores)
}
```

# Funcion que detecta si hubo compra de pescado o no. Es un segundo filtro que ayuda en la lectura e identificacion de los productos

```{r}
# ListaProductos = Lista 'recuperada' del filtro anterior
DetectaPescado <- function(ListaProductos){
  
  Pescado <- FALSE
  
  # ^ indica el inicio de la linea 
  # $ indica el final de la linea
  # Lo empleamos para que PESCADO sea el unico valor en la linea, y no confundir con otros productos
  
  if (any(grepl(x=ListaProductos, pattern='^PESCADO$'))) {
    
  Pescado <- TRUE
  indice_pescado <- grep(x=ListaProductos, pattern='^PESCADO$')
  ProductosPeso <- ListaProductos[(indice_pescado+1):length(ListaProductos)]
  Productos <- ListaProductos[0:(indice_pescado-1)]
  }
  
  # Pescado = Valor bool para saber si tiene pescado
  # Productos = Todos los productos unitarios, o por peso, si no tiene PESCADO
  # ProductosPeso = Los productos, pescados y, frutas y verdutas
  
  if (Pescado){
    valores <- list(Pescado, Productos, ProductosPeso)
  } else {
    valores <- list(Pescado, Productos)
  }
  
  return(valores)
}

```

# Patrones para diferenciar a los productos y sus respectivos precios

```{r}
# Sabemos que disponemos de 3 tipos de productos
TipoProducto <- c('Unitario', 'Pescado', 'Fruta y verdura')

# Extraer los valores de cada linea con el formato de cantidad, descripcion, preciounitario, importe. 
# - ^([0-9]+) - El primer elemento, que es un número.
# - Luego de un espacio, \\s , lee cualquier caracter, excepto salto de linea (.+?)
# - Luego de otro espacio, \\s , lee el valor unitario ([0-9]+,[0-9]{2})
# - Luego de otro espacio, \\s , lee el importe total ([0-9]+,[0-9]{2})$, indicando que es el final de la linea. 

# Formato normal, para productos unitarios con mas de una unidad
patron_normal <- "^([0-9]+)\\s(.+?)\\s([0-9]+,[0-9]{2})\\s+([0-9]+,[0-9]{2})$"

# Caso 2: Cuando cantidad es 1. Es decir, sin precio unitario
patron_cantidad_1 <- "^1\\s(.+?)\\s+([0-9]+,[0-9]{2})$"

# Caso 3: Productos por peso, tanto pescado como fruteria.
patron_pescado <- "^[A-ZÁÉÍÓÚÑ]+(?:\\s+[A-ZÁÉÍÓÚÑ]+)*$"
patron_peso <- "^([0-9]+)\\s(.+)$"
patron_precio_peso <- "^([0-9,]+)\\s*kg\\s*([0-9,]+)\\s*€/kg\\s*([0-9,]+)$"

```

# Creacion de funcion que registra datos de los productos si tiene pescado.

NOTA: Esta funcion solo sirve si se ha comprado pescado. Por eso se creo la funcion anterior para detectar si había pescado o no en la compra

```{r}
# InformacionCompra = lista recuperada de ExtraerInformacion
# Parking = valor bool recuperado de DetectaParking
# ListaProductosUnitarios = lista recuperada de la funcion DetectaPescado
# ListaProductosPeso = lista recuperada de la funcion DetectaPescado, siempre que se haya comprado pescado
# ProductosRegistrados = dataframe Compra, que contendrá todos los productos leidos

RegistraProductosConFiltro <- function (InformacionCompra, Parking, ListaProductosUnitarios, ListaProductosPeso, ProductosRegistrados){
  
  fecha <- InformacionCompra[[1]][[5]]
  idtienda <- InformacionCompra[[1]][[7]]
  idfactura <- InformacionCompra[[1]][[9]]
  
  # Tratamiento para los productos unitarios
  
  i <- 1
  
  while (i <= length(ListaProductosUnitarios)){
    
    p <- ListaProductosUnitarios[i]
    
    if (grepl(x=p, pattern=patron_cantidad_1)){
      
      detalleproducto <- str_match(string=p, pattern=patron_cantidad_1)[2:3]
      
      nombreproducto <- detalleproducto[1]
      precio <- detalleproducto[2]
      importe <- detalleproducto[2]
      tproducto <- TipoProducto[1]
      
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Producto = nombreproducto, 
                    Cantidad = '1', 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto, 
                    Parking = Parking)
      
      ProductosRegistrados <- rbind(ProductosRegistrados, valores)
      
    } else {
      # Si no sigue el patron de una unidad, seguirá el patrón de más de una unidad
      
      detalleproducto <- str_match(string=p, pattern=patron_normal)[2:5]
      
      cantidad <- detalleproducto[1]
      nombreproducto <- detalleproducto[2]
      precio <- detalleproducto[3]
      importe <- detalleproducto[4]
      tproducto <- TipoProducto[1]
      
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto,
                    Parking = Parking)
      
      ProductosRegistrados <- rbind(ProductosRegistrados, valores)
      
    }
    i <- i + 1
  }
  
  # Tratamiento para los datos por peso
  
  i <- 1
  
  while (i <= length(ListaProductosPeso)){
    
    p <- ListaProductosPeso[i]
    
    if (grepl(x=p, pattern=patron_pescado)){
      
      nombreproducto <- p
      
      i <- i + 1
      
      detalleproducto <- ListaProductosPeso[i]
      detalleproducto <- str_match(string=detalleproducto, pattern=patron_precio_peso)[2:4]
  
      cantidad <- detalleproducto[1]
      precio <- detalleproducto[2]
      importe <- detalleproducto[3]
      tproducto <- TipoProducto[2]
      
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto,
                    Parking = Parking)
      
      ProductosRegistrados <- rbind(ProductosRegistrados, valores)
      
    } else {
      
      # Si no sigue el patron de pescado, seguirá el patron de frutas y verduras; por peso. 
      
      nombreproductopeso <- str_match(string=p, pattern=patron_peso)[2:3]
      
      cantidad <- nombreproductopeso[1]
      nombreproducto <- nombreproductopeso[2]
      
      i <- i + 1
      
      detalleproducto <- ListaProductosPeso[i]
      detalleproducto <- str_match(string=detalleproducto, pattern=patron_precio_peso)[2:4]
  
      cantidad <- detalleproducto[1]
      precio <- detalleproducto[2]
      importe <- detalleproducto[3]
      tproducto <- TipoProducto[3]
      
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto, 
                    Parking = Parking)
      
      ProductosRegistrados <- rbind(ProductosRegistrados, valores)
    }
    i <- i + 1
  }
  
  # Solo devolverá el dataframe que contiene los productos registrados
  
  return(ProductosRegistrados)
}
```

# Creacion de funcion que registra datos de los productos cuando no se ha comprado pescado.

```{r}
RegistraProductosSinFiltro <- function (InformacionCompra, Parking, ListaProductos, ProductosRegistrados){
  
  fecha <- InformacionCompra[[1]][[5]]
  idtienda <- InformacionCompra[[1]][[7]]
  idfactura <- InformacionCompra[[1]][[9]]
  
  # Tratamiento para los productos unitarios

  i <- 1
    
  while (i <= length(ListaProductos)) {
    
    p <- ListaProductos[i]
    
    if (grepl(x=p, pattern=patron_normal) | grepl(x=p, pattern=patron_cantidad_1)){
      
      tproducto <- TipoProducto[1]
      
      if (grepl(x=p, pattern=patron_normal)) {
        
        # Productos que tienen mas de una cantidad 
        
        detalleproducto <- str_match(string=p, pattern=patron_normal)[2:5]
        
        cantidad <- detalleproducto[1]
        nombreproducto <- detalleproducto[2]
        precio <- detalleproducto[3]
        importe <- detalleproducto[4]
        
        valores <- data.frame(
                      Fecha = fecha, 
                      IdTienda = idtienda, 
                      IdFactura = idfactura, 
                      Producto = nombreproducto, 
                      Cantidad = cantidad, 
                      PrecioUnitario = precio, 
                      Importe = importe, 
                      TipoProducto = tproducto,
                      Parking = Parking)
        
        ProductosRegistrados <- rbind(ProductosRegistrados, valores)
        
      } else if (grepl(x=p, pattern=patron_cantidad_1)){
        
        # Productos que tienen una (1) cantidad 
          
        cantidad <- '1'
        
        detalleproducto <- str_match(string=p, pattern=patron_cantidad_1)[2:3]
        
        nombreproducto <- detalleproducto[1]
        precio <- detalleproducto[2]
        
        valores <- data.frame(
                      Fecha = fecha, 
                      IdTienda = idtienda, 
                      IdFactura = idfactura, 
                      Producto = nombreproducto, 
                      Cantidad = cantidad, 
                      PrecioUnitario = precio, 
                      Importe = precio, 
                      TipoProducto = tproducto,
                      Parking = Parking)
        
        ProductosRegistrados <- rbind(ProductosRegistrados, valores)
      }
      
    } else {
      
      # Si no tienen el patron normal o de una cantidad, entonces es un producto por peso 
      
      # Tratamiento producto por peso, para frutas y verduras
      
      tproducto <- TipoProducto[3]
      
      nombreproductopeso <- str_match(string=p, pattern=patron_peso)[2:3]
      
      cantidad <- nombreproductopeso[1]
      nombreproducto <- nombreproductopeso[2]
      
      i <- i + 1
      
      detalleproducto <- ListaProductos[i]
      detalleproducto <- str_match(string=detalleproducto, pattern=patron_precio_peso)[2:4]
  
      cantidad <- detalleproducto[1]
      precio <- detalleproducto[2]
      importe <- detalleproducto[3]
      
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto, 
                    Parking = Parking)
      
      ProductosRegistrados <- rbind(ProductosRegistrados, valores)
    }
    i <- i + 1
  }
  
  # Solo devolverá el dataframe que contiene los productos registrados
  return(ProductosRegistrados)
  
}

```

# General 

Funcion que almacena:

1. El nombre de los ficheros que no fueron leidos.

2. El nombre de los ficheros que fueron leidos; los que corresponden a Mercadona. 

3. Información de todas las tiendas que fueron leidas.

4. Información sobre productos y compras realizadas


```{r}
# Funcion para almacenar los ficheros leidos y los que no. 

# Le paso el dataframe de los ficheros leidos y no leidos para que los modifique, 
# si se añade un nuevo fichero. 

RegistroFicheros <- function(Nombrepdf, DF_FicherosNoLeidos, DF_FicherosLeidos, DF_Tiendas, DF_Compras) {
  
  Verifica <- EsMercadona(Nombrepdf)
  
  # Bool para saber si es de mercadona o no
  V <- Verifica[[1]]
  
  # Todas las lineas leidas
  Lineas <- verifica[[2]]
  
  Informacion <- ExtraeInformacion(lineas)
  
  # Todo el bloque que contiene los productos, incluido el Parking
  Productos <- informacion[[2]]
  
  # FILTRO 1
  filtro1 <- DetectaParking(Productos)
  
  # Valor bool para saber si tiene parking o no
  ValorParking <- filtro1[[1]]
  
  # Todo el bloque que contiene los productos sin el parking
  Productos <- filtro1[[2]]
  
  # FILTRO 2
  filtro2 <- DetectaPescado(Productos)
  
  # Valor bool para saber si tiene pescado o no
  Pescado <- filtro2[[1]]
  
  # Añadimos los ficheros que no fueron leidos correctamente o que no son de Mercadona. 
  # Servirá para hacer seguimiento sobre el fichero
  
  # INFORMACION GENERAL DE LOS FICHEROS 
  
  if ((V == FALSE) && !(Nombrepdf %in% DF_FicherosNoLeidos$NombreFichero)) {
    
    DF_FicherosNoLeidos <- DF_FicherosNoLeidos %>%
                                              add_row( NombreFichero = Nombrepdf )
    
  }
  
  # Si es de Mercadona 
  
  else if (V == TRUE) {
    
    direccion <- Informacion[[1]][[1]]
    codpos <- Informacion[[1]][[2]]
    municipio <- Informacion[[1]][[3]]
    tel <- Informacion[[1]][[4]]
    id_tienda <- Informacion[[1]][[7]]
    id_factura <- Informacion[[1]][[9]]
    
    # Añadimos los ficheros que fueron leidos correctamente y además, añadimos la factura a la que 
    # corresponde para verificar posteriormente
    
    if (!id_factura %in% DF_FicherosLeidos$FacturaFichero){
      
      DF_FicherosLeidos <- DF_ficheros_leidos %>%
                                              add_row(
                                                FacturaFichero = id_factura,
                                                NombreFichero = Nombrepdf
                                              )
    }
    
    if (!id_tienda %in% DF_Tiendas$IdTienda){
          DF_Tiendas <- DF_Tiendas %>%
                                    add_row(
                                      IdTienda = id_tienda,
                                      Direccion = direccion,
                                      CodigoPostal = codpos,
                                      Municipio = municipio,
                                      Telefono = tel)
    }
  }
  
  # INFORMACION SOBRE LOS PRODUCTOS COMPRADOS
  
  if (Pescado) {
    
    ProductosUnitarios <- filtro2[[2]]
    ProductosPeso <- filtro2[[3]]
    
    Compra <- RegistraProductosConFiltro( InformacionCompra = informacion,
                               Parking = ValorParking,
                               ListaProductosUnitarios = ProductosUnitarios,
                               ListaProductosPeso = ProductosPeso,
                               ProductosRegistrados = Compra)
    
    } else {
      
      Compra <- RegistraProductosSinFiltro ( InformacionCompra = informacion,
                                             Parking = ValorParking,
                                             ListaProductos = Productos,
                                             ProductosRegistrados = Compra)
    }
  
  valores <- list(DF_FicherosNoLeidos, DF_FicherosLeidos, DF_Tiendas, DF_Compras)
  # Devuelve todos los dataframes con la respectiva información
  return(valores)
}

```

# Verificacion de las funciones anteriores para todos los ficheros

## Carga de todos los ficheros

```{r}
#lista_ficheros <- list.files(path = 'data/', pattern = "\\.pdf$")
lista_nombres_ficheros <- list.files(path = 'data/')
length(lista_nombres_ficheros)
```

## Bucle para cargar la informacion de cada fichero

```{r}
for (f in lista_nombres_ficheros) {
  
  Resultado <- RegistroFicheros (Nombrepdf = f,
                                 DF_FicherosNoLeidos = NoLeido, 
                                 DF_FicherosLeidos = FicheroLeido, 
                                 DF_Tiendas = Tienda, 
                                 DF_Compras = Compra)
  
  NoLeido <- resultado$DF_FicherosNoLeidos
  FicheroLeido <- resultado$DF_FicherosLeidos
  Tienda <- resultado$DF_Tiendas
  Compra <- resultado$DF_Compras
}

```

Una vez que recuperamos la informacion sobre las facturas leidas y no leias, verificamos

```{r}
lista_f_almacenados <- c(NoLeido$NombreFichero, FicheroLeido$NombreFichero)
diferencia <- setdiff(lista_nombres_ficheros, lista_f_almacenados)
#diferencia

```
De estos nombres, identificamos que se trata de ficheros repetidos. Ya fueron leidos anteriormente y por tanto, no se vuelven a leer ni almacenar. El caso del 'ticket3.pdf' es un caso especial, ya que al ser escaneado, lo detecta como MERCADONA, pero no lee lo valores correctamente. Por eso no es considerado dentro de los df's correspodientes.


```{r}
#x <- lista_nombres_ficheros[2]
#x <- '20240323 Mercadona 234,20.pdf'# Pescado 
x <- '20231205 Mercadona 58,00.pdf' # Sin Pescado

# <- "ticket3.pdf"

verifica <- EsMercadona(x)

lineas <- verifica[[2]]

r <- RegistroFicheros(Nombrepdf = x,NoLeido, FicheroLeido, Tienda)
NoLeido <- r$NoLeido
FicheroLeido <- r$FicheroLeido
Tienda <- r$TiendaLeida

productos <- ExtraeInformacion(lineas)[[2]]
productos

cabeza <- ExtraeInformacion(lineas)[[1]]

```

Ya tenemos identificados los productos. Ahora sigue determinar:

1. Si tiene parking o no
2. Qué tipo de producto es

```{r}
# Sabemos que disponemos de 3 tipos de productos
tipo_producto <- c('Unitario', 'Pescado', 'Fruta y verdura')

# Los productos al peso, como pescado, frutas y verduras están al final. Sabiendo que pescado está en primer lugar, podemos hacer un primer filtro. 

# Extraer los valores de cada linea con el formato de cantidad, descripcion, preciounitario, importe. 
# - ^([0-9]+) - El primer elemento, que es un número.
# - Luego de un espacio, \\s , lee cualquier caracter, excepto salto de linea (.+?)
# - Luego de otro espacio, \\s , lee el valor unitario ([0-9]+,[0-9]{2})
# - Luego de otro espacio, \\s , lee el importe total ([0-9]+,[0-9]{2})$, indicando que es el final de la linea. 

# Formato normal, para productos unitarios con mas de una unidad
patron_normal <- "^([0-9]+)\\s(.+?)\\s([0-9]+,[0-9]{2})\\s+([0-9]+,[0-9]{2})$"

# Caso 2: Cuando cantidad es 1. Es decir, sin precio unitario
patron_cantidad_1 <- "^1\\s(.+?)\\s+([0-9]+,[0-9]{2})$"

# Caso 3: Productos por peso, tanto pescado como fruteria.
patron_pescado <- "^[A-ZÁÉÍÓÚÑ]+(?:\\s+[A-ZÁÉÍÓÚÑ]+)*$"
patron_peso <- "^([0-9]+)\\s(.+)$"
patron_precio_peso <- "^([0-9,]+)\\s*kg\\s*([0-9,]+)\\s*€/kg\\s*([0-9,]+)$"

```

# Aplicamos filtros para saber el tipo de producto.

```{r}
# Como parking está al final del todo, es el filtro que aplicamos primero.
DetectaParking <- function(ListaProductos){
  
  parking <- FALSE
  
  if (any(grepl(x=ListaProductos, pattern='PARKING'))) {
    parking <- TRUE
    indice_parking <- grep(x=ListaProductos, pattern='PARKING')
    ListaProductos <- ListaProductos[1:(indice_parking-1)]
  }
  valores <- list(ListaProductos, parking)
  return(valores)
}

```


```{r}
DetectaParking(productos)[[2]] # Valor bool para saber si tiene parking o no
DetectaParking(productos)[[1]] # Lista de productos con el parking descartado
```

Dato que tenemos orden de productos: 

1. Unitario
2. Pescado
3. Frutas y verduras

Aplicamos un primer filtro para los pescados y, frutas y verduras. 

# Funcion que determina el tipo de alimento y lo almacena en el df de compra. 

```{r}
#productos

# Primer filtro
resultados <- Compra

fecha <- cabeza[[5]]
idtienda <- cabeza[[7]]
idfactura <- cabeza[[9]]

if (any(grepl(x=productos, pattern='PESCADO'))) {
  
  Pescado <- TRUE
  indice_pescado <- grep(x=productos, pattern='PESCADO')
  productos_peso <- productos[(indice_pescado+1):length(productos)]
  productos <- productos[0:(indice_pescado-1)]
  
}

# Esto solo funciona si Pescado

if (Pescado){ 
  i <- 1
  
  while (i <= length(productos_peso)){
    
    p <- productos_peso[i]
    
    if (grepl(x=p, pattern=patron_pescado)){
      
      nombreproducto <- p
      
      i <- i + 1
      
      detalleproducto <- productos_peso[i]
      
      if (grepl(x=detalleproducto, pattern=patron_precio_peso)){
        
        detalleproducto <- str_match(detalleproducto, patron_precio_peso)[2:4]
  
        idtienda <- cabeza[[7]]
        idfactura <- cabeza[[9]]
        fecha <- cabeza[[5]]
        cantidad <- detalleproducto[1]
        precio <- detalleproducto[2]
        importe <- detalleproducto[3]
        tproducto <- tipo_producto[2]
      }
      
      # Ya tenemos identificados los pescados
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto, 
                    Parking = parking)
      
      resultados <- rbind(resultados, valores)
      
    }
    
    else if (grepl(x=p, pattern=patron_peso)) { # si el producto no es pescado, será fruta y verdura
      
      cantidad_nombre_fruta <- str_match(string=p, pattern=patron_peso)[2:3]
      
      cantidad <- cantidad_nombre_fruta[1]
      nombreproducto <- cantidad_nombre_fruta[2]
      
      i <- i + 1
      
      detalleproducto <- productos_peso[i]
      
      if (grepl(x=detalleproducto, pattern=patron_precio_peso)){
      
      detalleproducto <- str_match(detalleproducto, patron_precio_peso)[2:4]
      idtienda <- cabeza[[7]]
      idfactura <- cabeza[[9]]
      fecha <- cabeza[[5]]
      cantidad <- detalleproducto[1]
      precio <- detalleproducto[2]
      importe <- detalleproducto[3]
      tproducto <- tipo_producto[3]
      }
      
      # Ya tenemos identificados las frutas y verduras
      valores <- data.frame(
              Fecha = fecha, 
              IdTienda = idtienda, 
              IdFactura = idfactura, 
              Producto = nombreproducto, 
              Cantidad = cantidad, 
              PrecioUnitario = precio, 
              Importe = importe, 
              TipoProducto = tproducto, 
              Parking = parking)
          
      resultados <- rbind(resultados, valores)
  
  } else {
    
    p <- productos[i]
    
    if (grepl(x=p, pattern=patron_normal) | grepl(x=p, pattern=patron_cantidad_1)){
      
      tproducto <- tipo_producto[1]
      
      if (grepl(x=p, pattern=patron_normal)) {
        
        detalleproducto <- str_match(string=p, pattern=patron_normal)[2:5]
        
        idtienda <- cabeza[[7]]
        idfactura <- cabeza[[9]]
        fecha <- cabeza[[5]]
        cantidad <- detalleproducto[1]
        nombreproducto <- detalleproducto[2]
        precio <- detalleproducto[3]
        importe <- detalleproducto[4]
        
        
      } else if (grepl(x=p, pattern=patron_cantidad_1)){
        
        cantidad <- '1'
        
        detalleproducto <- str_match(string=p, pattern=patron_cantidad_1)[2:3]
        
        idtienda <- cabeza[[7]]
        idfactura <- cabeza[[9]]
        fecha <- cabeza[[5]]
        nombreproducto <- detalleproducto[1]
        precio <- detalleproducto[2]
        importe <- detalleproducto[2]
        
      }
        
      # Ya tenemos identificados los productos unitarios
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto, 
                    Parking = parking)
      
      resultados <- rbind(resultados, valores)
      }
    }
  i <- i + 1 
}
} else {
  # No hay pescado
  
  i <- 1
  
  while (i <= length(productos)) {
    
    p <- productos[i]
    
    if (grepl(x=p, pattern=patron_normal) | grepl(x=p, pattern=patron_cantidad_1)){
      
      tproducto <- tipo_producto[1]
      
      if (grepl(x=p, pattern=patron_normal)) {
        
        detalleproducto <- str_match(string=p, pattern=patron_normal)[2:5]
        idtienda <- cabeza[[7]]
        idfactura <- cabeza[[9]]
        fecha <- cabeza[[5]]
        cantidad <- detalleproducto[1]
        nombreproducto <- detalleproducto[2]
        precio <- detalleproducto[3]
        importe <- detalleproducto[4]
        
      } else if (grepl(x=p, pattern=patron_cantidad_1)){
        
        cantidad <- 1
        
        detalleproducto <- str_match(string=p, pattern=patron_cantidad_1)[2:3]
        
        idtienda <- cabeza[[7]]
        idfactura <- cabeza[[9]]
        fecha <- cabeza[[5]]
        nombreproducto <- detalleproducto[1]
        precio <- detalleproducto[2]
        importe <- detalleproducto[2]
        
      }
      
      # Ya tenemos identificados los productos unitarios de una unidad
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto, 
                    Parking = parking)
      
      resultados <- rbind(resultados, valores)
    } else if (grepl(x=p, pattern=patron_peso)) { # si el producto no es pescado, será fruta y verdura
      
      cantidad_nombre_fruta <- str_match(string=p, pattern=patron_peso)[2:3]
      
      cantidad <- cantidad_nombre_fruta[1]
      nombreproducto <- cantidad_nombre_fruta[2]
      
      i <- i + 1
      
      detalleproducto <- productos_peso[i]
      
      if (grepl(x=detalleproducto, pattern=patron_precio_peso)){
      
      detalleproducto <- str_match(detalleproducto, patron_precio_peso)[2:4]
      idtienda <- cabeza[[7]]
      idfactura <- cabeza[[9]]
      fecha <- cabeza[[5]]
      cantidad <- detalleproducto[1]
      precio <- detalleproducto[2]
      importe <- detalleproducto[3]
      tproducto <- tipo_producto[3]
      }
      
      # Ya tenemos identificados las frutas y verduras
      valores <- data.frame(
              Fecha = fecha, 
              IdTienda = idtienda, 
              IdFactura = idfactura, 
              Producto = nombreproducto, 
              Cantidad = cantidad, 
              PrecioUnitario = precio, 
              Importe = importe, 
              TipoProducto = tproducto, 
              Parking = parking)
          
      resultados <- rbind(resultados, valores)
  
  }
  i <- i + 1
  }
}

productos

```

# Filtro sin pescado

```{r}
x <- '20231205 Mercadona 58,00.pdf' # Sin Pescado

verifica <- EsMercadona(x)

lineas <- verifica[[2]]

r <- RegistroFicheros(Nombrepdf = x,NoLeido, FicheroLeido, Tienda)
NoLeido <- r$NoLeido
FicheroLeido <- r$FicheroLeido
Tienda <- r$TiendaLeida

productos <- ExtraeInformacion(lineas)[[2]]
productos
```

```{r}
Pescado <- FALSE

cabeza <- ExtraeInformacion(lineas)[[1]]

resultados <- Compra

parking <- DetectaParking(productos)[[2]] # Valor bool para saber si tiene parking o no

i <- 1
  
while (i <= length(productos)) {
  
  p <- productos[i]
  
  if (grepl(x=p, pattern=patron_normal) | grepl(x=p, pattern=patron_cantidad_1)){
    
    tproducto <- tipo_producto[1]
    
    if (grepl(x=p, pattern=patron_normal)) {
      
      detalleproducto <- str_match(string=p, pattern=patron_normal)[2:5]
      idtienda <- cabeza[[7]]
      idfactura <- cabeza[[9]]
      fecha <- cabeza[[5]]
      cantidad <- detalleproducto[1]
      nombreproducto <- detalleproducto[2]
      precio <- detalleproducto[3]
      importe <- detalleproducto[4]
      
      # Ya tenemos identificados los productos unitarios normales
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto, 
                    Parking = parking)
      
      resultados <- rbind(resultados, valores)
      
    }
      else if (grepl(x=p, pattern=patron_cantidad_1)){
        
        cantidad <- 1
        
        detalleproducto <- str_match(string=p, pattern=patron_cantidad_1)[2:3]
        
        idtienda <- cabeza[[7]]
        idfactura <- cabeza[[9]]
        fecha <- cabeza[[5]]
        nombreproducto <- detalleproducto[1]
        precio <- detalleproducto[2]
        importe <- detalleproducto[2]
        
        # Ya tenemos identificados los productos unitarios de una unidad
        valores <- data.frame(
                      Fecha = fecha, 
                      IdTienda = idtienda, 
                      IdFactura = idfactura, 
                      Producto = nombreproducto, 
                      Cantidad = cantidad, 
                      PrecioUnitario = precio, 
                      Importe = importe, 
                      TipoProducto = tproducto, 
                      Parking = parking)
        
        resultados <- rbind(resultados, valores)
      }
  }

  
  i <- i + 1
  
}

```



















# Codigo funcional para una sola factura

Lectura de un solo fichero pdf, de prueba. 

Es una factura del mercadona? 
```{r}
# Carga de los pdf 



#nombre_pdf <- 'data/ticket_dia_03_02_2025_14_10.pdf' # Factura de otro supermercado
nombre_pdf <- 'data/20240323 Mercadona 234,20.pdf' # Factura con pescado
#nombre_pdf <- 'data/20241228 Mercadona 32,50 Ôé¼.pdf'
#nombre_pdf <- 'data/20250224 Mercadona 75,48 Ôé¼.pdf' # Con Parking



pdf_completo <- pdf_text(nombre_pdf) # Extrae el texto del pdf.
lineas <- strsplit(pdf_completo, "\n")[[1]] # Divide el texto en diferentes lineas, por cada salto de linea. 

#lineas <- sub("^\\s+", "", lineas) # Eliminamos los espacios en blanco a la izq de cada linea.
lineas <- trimws(lineas) # Elimina espacios en blanco al inicio y final de cada linea.


# Verificamos que sea una factura de Mercadona.

Mercadona <- grepl('MERCADONA', lineas[1])  
Mercadona # Solo si es TRUE, debe continuar. De no ser así, es que la factura no es de Mercadona.

```

Información encabezado, FIJO. 

Solo si es una factura del Mercadona:

```{r}
# De ser una factura del mercadona (Mercadona == TRUE), continuamos...

if (Mercadona) {
  
  info_compra <- grep(paste0("\\b", 'MERCADONA', "\\b"), lineas, ignore.case = TRUE)[1]
  info_compra_final <- grep(paste0("\\b", 'FACTURA', "\\b"), lineas, ignore.case = TRUE)[1]
  
  encabezado <- lineas[info_compra:info_compra_final]
  
}

encabezado

```
Una vez que tenemos identificado el 'espacio' del encabezado, extraemos la información. 

```{r}
if (Mercadona) {
  
  direccion <- encabezado[2]
  
  # Ubicacion 
  ubicacion <- strsplit(lineas[3], split = ' ')[[1]] 
  
  CP <- ubicacion[1]
  
  municipio <- ubicacion[2]
  
  telefono <- strsplit(lineas[4], split = ': ')[[1]][2]
  
  tiempo <- strsplit(lineas[5], split = ' ')[[1]][1:2]
  
  fecha <- tiempo[1] # String de la fecha en formato dd/mm/aaaa
  fecha
  
  hora <- tiempo[2]
  hora
  
  factura <- strsplit(lineas[6], split = ": ")[[1]]
  factura <- strsplit(factura[2], split = "-")[[1]]
  
  id_tienda <- factura[1]
  id_tienda
  
  n_caja <- factura[2]
  n_caja
  
  id_factura <- factura[3]
  id_factura
  
}

# Añadimos información del fichero, si pertenece a mercadona o no.

if ((Mercadona) & (!id_factura %in% FicheroLeido$FacturaFichero)) {
  FicheroLeido <- FicheroLeido %>%
                add_row(
                  NombreFichero = nombre_pdf,
                  FacturaFichero = id_factura
                )
} else if ((!Mercadona) & ((!nombre_pdf %in% NoLeido$NombreFichero))){
    NoLeido <- NoLeido %>%
                add_row(
                  NombreFichero = nombre_pdf
                )
}

# La información leida anteriormente, la concatenaremos en el df anteriormente creado. 
# Solo si el id_tienda no está.

info_tienda <- c(id_tienda, direccion, CP, municipio, telefono)
info_tienda

if ((Mercadona) & (!id_tienda %in% Tienda$IdTienda)){
  Tienda <- Tienda %>%
                add_row(
                  IdTienda = id_tienda,
                  Direccion = direccion,
                  CodigoPostal = CP,
                  Municipio = municipio,
                  Telefono = telefono
    )
}

```
NOTA: Automatizar para que lea cada fichero de la carpeta y almacene la información en los dataframes correspondientes. Corregir para que en nombre del fichero no aparezca 'data/'.


# Información de los productos.
```{r}
# Como sabemos que los poductos se describen desde el encabezado 'descripcion', hasta el primer 'TOTAL', buscamos la linea que contenga esa palabra. Por lo tanto:

indice_encabezado <- grep(paste0("\\b", 'Descripción', "\\b"), lineas, ignore.case = TRUE)[1]
indice_total <- grep(paste0("\\b", 'TOTAL', "\\b"), lineas, ignore.case = TRUE)[1]

# Leemos los productos, que van desde una linea despues del encabezado descripcion, hasta una linea menos del primer total. 

productos <- lineas[(indice_encabezado+1):(indice_total-1)]
productos <- str_replace_all(productos, "\\s+", " ") # Eliminamos los espacios multiples
productos

```

# Identificamos el patron de los diferentes productos, ya que pueden ser de tipo unitario, o por peso.
```{r}
# Sabemos que disponemos de 3 tipos de productos
tipo_producto <- c('Unitario', 'Pescado', 'Fruta y verdura')

# Los productos al peso, como pescado, frutas y verduras están al final. Sabiendo que pescado está en primer lugar, podemos hacer un primer filtro. 

# Extraer los valores de cada linea con el formato de cantidad, descripcion, preciounitario, importe. 
# - ^([0-9]+) - El primer elemento, que es un número.
# - Luego de un espacio, \\s , lee cualquier caracter, excepto salto de linea (.+?)
# - Luego de otro espacio, \\s , lee el valor unitario ([0-9]+,[0-9]{2})
# - Luego de otro espacio, \\s , lee el importe total ([0-9]+,[0-9]{2})$, indicando que es el final de la linea. 

# Formato normal (cantidad > 1)
patron_normal <- "^([0-9]+)\\s(.+?)\\s([0-9]+,[0-9]{2})\\s+([0-9]+,[0-9]{2})$"

# Caso 2: Cuando cantidad es 1 (sin precio unitario)
patron_cantidad_1 <- "^1\\s(.+?)\\s+([0-9]+,[0-9]{2})$"

# Caso 3: Productos por peso, tanto pescado como fruteria.
patron_pescado <- "^[A-ZÁÉÍÓÚÑ]+(?:\\s+[A-ZÁÉÍÓÚÑ]+)*$"
patron_peso <- "^([0-9]+)\\s(.+)$"
patron_precio_peso <- "^([0-9,]+)\\s*kg\\s*([0-9,]+)\\s*€/kg\\s*([0-9,]+)$"

```

# Aplicamos los diferentes filtros para determinar el tipo de producto.

```{r}
# FORMATO

# Unitarios
# Pescado
# Futeria
# Parking

# Como sabemos que parking solo estará en las ultimas dos lineas, lo descartamos primero. 
# Con esto sabemos si tiene parking. 

parking <- FALSE

if (any(grepl(paste0("\\b", 'PARKING', "\\b"), productos, ignore.case = TRUE))) {
  parking <- TRUE
  indice_parking <- grep(paste0("\\b", 'PARKING', "\\b"), productos, ignore.case = TRUE)[1]
  productos <- productos[1:(indice_parking-1)]
}

productos

```


```{r}
# Solo si se ha comprado pescado, servira este filtro, para diferenciar entre unitarios y por peso.

Peso <- FALSE

if (any(grepl('\\bPESCADO\\b', productos, ignore.case = TRUE))) {
  Peso <- TRUE
  indice_pescado <- grep("\\bPESCADO\\b", productos, ignore.case = TRUE)[1]
  productos_peso <- productos[(indice_pescado+1):length(productos)]
  productos <- productos[0:(indice_pescado-1)]
} 

productos
productos_peso

```

# Tratamos productos por peso
```{r}
# En caso de haber productos por peso, identificamos:

if (Peso) {
  
  ListaCompra <- list()
  indice <- 1 # indice de la lista para añadir los productos  
  i <- 1
  
  while (i <= length(productos_peso)){
    p <- productos_peso[i]
    
    if (grepl(patron_pescado, p)) {
      elemento1 <- p
      
      if (i + 1 <= length(productos_peso)){
        i <- i + 1 
        info <- productos_peso[i]
        elemento2 <- str_match(info, patron_precio_peso)[2:4]
        
        # Una vez tenemos info del producto, lo añadimos a la lista 
        
        ListaCompra[[indice]] <- list( Fecha = fecha,
                                       IdTienda = id_tienda,
                                       IdFactura = id_factura,
                                        Producto = elemento1,
                                        Cantidad = elemento2[1],
                                        PrecioUnitario = elemento2[2],
                                        Importe = elemento2[3],
                                        TipoProducto = tipo_producto[2],
                                        Parking = parking )
        indice <- indice + 1
      }
    }
    
    else if (grepl(patron_peso, p)) {
        
        elemento1 <- str_match(p, patron_peso)[,3]
        
        if (i + 1 <= length(productos_peso)){
          i <- i + 1 
          info <- productos_peso[i]
          elemento2 <- str_match(info, patron_precio_peso)[2:4]
          ListaCompra[[indice]] <- list(  Fecha = fecha,
                                          IdTienda = id_tienda,
                                          IdFactura = id_factura,
                                          Producto = elemento1,
                                          Cantidad = elemento2[1],
                                          PrecioUnitario = elemento2[2],
                                          Importe = elemento2[3],
                                          TipoProducto = tipo_producto[3],
                                          Parking = parking)
          indice <- indice + 1
          }
    }
    
    i <- i + 1 
    
  }
  
}

Compra <- bind_rows(Compra, bind_rows(ListaCompra))

Compra

```
