---
title: "Análisis de Tickets de Supermercado: Mini Proyecto 2025"
author: |
  <center>
  Ferrer Font, Neus<br>
  Navarro García, Marta<br>
  Rodríguez Simón, Alba<br>
  Aissa Djellouli, Mohammed<br>
  Balbastre Aparisi, Héctor<br>
  Ruiz Ripoll, Joan<br>
  Martinez Torres, Carlos Santiago
  </center>
date: "`r Sys.Date()`"
output:
  bookdown::pdf_document2:
    toc: true
    toc_depth: 3
    number_sections: true
    figure_caption: "Figura" # Referencias en castellano
    table_caption: "Tabla"
  bookdown::html_document2:
    echo: true
    number_sections: true
    theme: spacelab
    toc: true
    figure_caption: "Figura"
    table_caption: "Tabla"
  prettydoc::html_pretty:
    theme: cayman
    highlight: github
  html_notebook:
    echo: true
    number_sections: true
    toc: true
  pdf_document: default
  html_document:
    echo: true
    number_sections: true
    theme: lumen
    toc: true
always_allow_html: true
params:
  lang: ES
lang: "`r switch(params$lang, ES = 'es-ES', EN = 'en-US')`"
subtitle: "Tratamiento de datos. Grado en Ciencia de Datos - UV"
language:
  label:
    fig: 'Figura '
    tab: 'Tabla '
    eq: 'Ecuación '
    thm: 'Teorema '
    lem: 'Lema '
    def: 'Definición '
    cor: 'Corolario '
    prp: 'Proposición '
    exm: 'Ejemplo '
    exr: 'Ejercicio '
    proof: 'Demostración. '
    remark: 'Nota: '
    solution: 'Solución. '
---

# Introducción

En este proyecto se plantea realizar el análisis de tickets del supermercado Mercadona, los cuales fueron recolectados por los estudiantes del grado de Ciencia de datos y otros de cursos anteriores.

## Contexto

Actualmente, las grandes empresas gestionan volúmenes significativos de productos, lo que conlleva elevados costes asociados al transporte y almacenamiento. Por este motivo, resulta fundamental optimizar cada etapa del proceso logístico. Con base en nuestro conocimiento en Big Data, nuestro equipo se enfocará en analizar y comprender las tendencias de los consumidores, con el objetivo de reducir costes operativos.

## Grupo

Nuestro objetivo al responder estas preguntas es profundizar en los hábitos y preferencias reales de los consumidores a partir del análisis de tickets de compra. Al extraer patrones y correlaciones dentro de estos datos, buscamos generar conocimiento útil para anticipar comportamientos, mejorar la planificación comercial y ajustar la oferta a la demanda real. Esta aproximación nos permitirá aportar valor estratégico desde el análisis de datos, con el foco puesto en reducir ineficiencias, mejorar la toma de decisiones y apoyar la sostenibilidad operativa del negocio desde una perspectiva basada en la evidencia.

## Preguntas planteadas

1)¿Qué frutas tienen mayor venta en primavera?

2)¿Qué eventos locales o festivos generan picos de venta significativos? (Ej. Navidad)

3)¿Existen diferencias significativas en las preferencias de compra según zonas (urbana vs rural)?

4)¿Qué categorías tienen mayor tendencia a generar compras adicionales (por ejemplo, comprar galletas con café)?

5)¿Qué productos muestran mayor consistencia de compra independientemente de cambios en su precio?

6)¿Cuántos productos compra en promedio una persona por ticket?

7)¿Cuál es el gasto promedio por compra y Cómo varía según el día/hora?

8)¿Qué categorías de productos (fruta, verdura… )suelen comprarse juntas con mayor frecuencia?

9)¿Hay diferencias de consumo en días laborales vs. fines de semana?

10)¿Cuál es el mejor horario para comprar? (Menos concurrencia)

11)¿Cuál es el mejor día para comprar? (Menos concurrencia)

12)¿Qué mes es mejor para comprar x fruta? (El mes con mayor compra de frutas y verduras)

13)¿Cuál ha sido el producto, unitario, más comprado y cuál ha sido el cambio de su precio a lo largo del tiempo?

14)¿Cuáles son los productos con mayor facturación total? (Top 5 de los más facturados)

15)¿Cómo varían las ventas a lo largo del mes? ¿Hay más compras a fin de mes?

16)¿Cuál es la tasa de personas que al comprar, tienen parking? (Compras con parking/Total de compras)

17)¿Qué productos aparecen con mayor frecuencia en los tickets de alto valor (€)?

18)¿Qué combinaciones de productos se repiten con más frecuencia en una misma compra?

# Material y métodos

## Carga de librerias

Para la carga de librerías, haremos uso del paquete pacman de R, que es eficiente para la carga e instalación de las mismas. En este proyecto, utilizaremos las librerias 'pdftools', 'tidyverse', 'readxl', 'lubridate', 'visdat' y 'knit'.

```{r include=FALSE}
# Limpiamos el entorno
rm(list = ls())
```

```{r include=FALSE, message=FALSE, warning=FALSE}
# Instalacion de librerias con pacman

# Asegurarse de que el paquete "pacman" está instalado
if (!require('pacman')) install.packages('pacman')

# Librerias a cargar
pacman::p_load(pdftools, tidyverse, readxl, lubridate, visdat, knitr)

# Con tidyverse: ggplot2, dplyr, tidyr, readr, purrr, tibble, stringr, forcats
# readxl, libreria que requiere una carga aparte explícita
# lubridate, se utiliza con frecuencia con paquetes de tidyverse, pero requiere una carga aparte
# visdat, exploracion visual de los datos 
```

## Carga de datos

Todos nuestros datos están almacenados la carpeta $.data$, y fueron obtenidos posteriormente desde un repositorio en la nube. Todos los ficheros contenidos en esta carpeta están en formato $.pdf$.

Antes de empezar con la extracción de los datos, fue necesario preguntarse: ¿Como vamos a almacenar la información? ¿Qué informacion queremos almacenar? Adicionalmente, para evitar problemas con la extracción y lectura de datos desde los ficheros pdf al entorno de R, se estableció que lo mejor sería tratar todos los datos como caracteres. Una vez que todos los datos fueran leídos correctamente, se podría hacer la conversión al tipo de dato correspondiente.

```{r include=FALSE}

# ¿Como vamos a almacenar la información? ¿Qué informacion queremos almacenar? 

# Dataframe que contendrá el nombre del fichero leido y la factura a la que pertenece. 

FicheroLeido <- data.frame( FacturaFichero = character(),
                        NombreFichero = character(),
                        stringsAsFactors = FALSE)

# En caso de no ser leido, porque no pertenece a Mercadona, guardaremos cual fichero ha sido 
# y lo almacenaremos en otro df, para identificarlo posteriormente.

NoLeido <- data.frame( NombreFichero = character(),
                        stringsAsFactors = FALSE)

# Dataframe que contendrá la información de las diferentes tiendas de las facturas.  

Tienda <- data.frame( IdTienda = character(),
                      Direccion = character(),
                      CodigoPostal = character(),
                      Municipio = character(),
                      Telefono = character(),
                      stringsAsFactors = FALSE )

# Dataframe que contendra la informacion de las compras realizadas. 

Compra <- data.frame( Fecha = character(),
                      IdTienda = character(),
                      IdFactura = character(),
                      Hora = character(),
                      Producto = character(),
                      Cantidad = character(),
                      PrecioUnitario = character(),
                      Importe = character(),
                      TipoProducto = character(),
                      Parking = logical(), 
                      stringsAsFactors = FALSE ) 

```

Nota: A lo largo de la extracción de los datos, se implementaron funciones para ser recursivos, además de facilitar la lectura del código y hacer un barrido completo sobre los ficheros.

```{r include=FALSE}

# Funcion EsMercadona, que comprueba si la factura es de Mercadona o no

EsMercadona <- function(NombrePDF){
  
  # Recibe el nombre el fichero en formato pdf
  
  carpeta <- 'data/'
  fichero <- paste0(carpeta, NombrePDF)
  
  texto <- pdf_text(fichero) # Extrae el texto del pdf.
  
  #texto <- iconv(texto, from = "latin1", to = "UTF-8") # Corregimos el error para valores extraños.

  lineas <- strsplit(texto, "\n")[[1]] # Divide el texto en diferentes lineas, por cada salto de linea.
  lineas <- trimws(lineas) # Elimina espacios en blanco al inicio y final de cada linea.
  
  # Para verificar que es de Mercadona y que se ha leido correctamente la factura, vamos a evaluar la primera linea.
  
  Mercadona <- grepl('MERCADONA', lineas[1])  # Valor bool para saber si es de Mercadona. 
  CIF <- grepl('A-46103834', lineas[1]) # Valor bool para saber si reconoce el CIF de Mercadona.
  
  if ((!Mercadona) | (!CIF)){
    Mercadona <- FALSE # Esto es preventivo, para el caso del ticket3.pdf
    lineas <- list()
  } 
  
  # Mercadona = Valor bool
  # lineas = lista de las lineas leidas; sera vacia en caso de no ser de Mercadona
  
  Informacion <- list(Mercadona, lineas)
  
  return(Informacion)
}
```

Dentro de cada fichero, encontramos dos partes fundamentales. La primera, es el encabezado. Contiene información sobre la compra en general. Por ejemplo, reune datos como la ubicación de la tienda, fecha y hora de compra, y número de ticket. En la segunda parte, nos encontramos con el bloque de los productos. En ella se describe el producto comprado, la cantidad y el importe pagado.

```{r include=FALSE}

# Funcion que lee la informacion de la tienda y de los productos por factura

# lineas 'recuperadas' de la funcion EsMercadona
ExtraeInformacion <- function(Lineas) {
  
    # Obtenemos el rango de filas que contienen el encabezado, fijo

    info_compra <- grep(x=Lineas, pattern='MERCADONA')[[1]]
    info_compra_final <- grep(x=Lineas, pattern='FACTURA')[[1]]
  
    # Definimos el encabezado y extraemos informacion 
    
    encabezado <- Lineas[info_compra:info_compra_final]
    
    direccion <- encabezado[2]

    # Ubicacion (CP y municipio)
    ubicacion <- strsplit(encabezado[3], split = ' ')[[1]] 
    
    CP <- ubicacion[1]
    municipio <- ubicacion[2]
    
    telefono <- strsplit(encabezado[4], split = ': ')[[1]][2]
    
    # Tiempo (fecha y hora)
    tiempo <- strsplit(encabezado[5], split = ' ')[[1]][1:2]
    
    fecha <- tiempo[1] # String de la fecha en formato dd/mm/aaaa
    hora <- tiempo[2] # 00:00
    
    # Factura (tienda, caja, idfactura)
    factura <- strsplit(encabezado[6], split = ": ")[[1]]
    factura <- strsplit(factura[2], split = "-")[[1]]
    
    id_tienda <- factura[1]
    n_caja <- factura[2]
    id_factura <- factura[3]
    
    # Esta es la informacion del encabezado. Fija para cada factura 
    InformacionCompra <- list(direccion, CP, municipio, telefono, fecha, hora, id_tienda, n_caja, id_factura)
    
    # Obtenemos el rango de filas que contienen los productos
    
    # ^Descripci ayudará a evitar errores con la tilde, por el momento
    
    indice_inicio_productos <- grep(x=Lineas, pattern='^Descripci')[[1]]
    indice_final_productos <- grep(x=Lineas, pattern='TOTAL')[[1]]
    
    inicio <- indice_inicio_productos+1
    final <- indice_final_productos-1
    
    Productos <- Lineas[inicio:final]
    Productos <- str_replace_all(Productos, "\\s+", " ") # Eliminamos los espacios multiples
    
    
    # InformacionCompra = lista con la informacion del encabezado
    # Productos = lista con los productos que contiene la factura
    valores <- list(InformacionCompra, Productos)
    
    return(valores)
}
```

Para la lectura de los productos, identificamos dos filtros esenciales. El primero, es si el ticket incluía parking o no. Este 'producto' estaba listado al final, lo que nos permitía de alguna forma reducir el listado de productos e identificar si había o no parking.

```{r include=FALSE}

# Filtro 1
## Funcion que detecta si hay parking o no 

# Como parking está al final del todo, es el filtro que aplicamos primero.
# ListaProductos = lista 'recuperada' de ExtraerInformacion
DetectaParking <- function(ListaProductos){
  
  Parking <- FALSE
  
  if (any(grepl(x=ListaProductos, pattern='PARKING'))) {
    Parking <- TRUE
    indice_parking <- grep(x=ListaProductos, pattern='PARKING')
    final <- indice_parking-1
    ListaProductos <- ListaProductos[1:final]
  }
  
  # Parking = Valor bool para saber si tiene parking o no

  valores <- list(Parking, ListaProductos)
  return(valores)
}
```

El segundo filtro, permitía saber si se compró pescado o no. Este filtro fue de utilidad dado que por la naturaleza de negocio de Mercadona, los tickets y los productos vienen clasificados en 3 categorías:

-   Venta por unidad.
-   Venta al peso, pescado.
-   Venta al peso, frutas y verduras.

Si en el ticket se registró la compra de pescado, ya podíamos establecer que a partir de ese filtro, los productos que estuvieran por debajo de esa línea, serían al peso. Y los que no, serían por unidad.

```{r include=FALSE}

# Filtro 2
## Funcion que detecta si hubo compra de pescado o no. Ayuda en la lectura e identificacion de los productos

# ListaProductos = Lista 'recuperada' del filtro anterior
DetectaPescado <- function(ListaProductos){
  
  Pescado <- FALSE
  
  # ^ indica el inicio de la linea 
  # $ indica el final de la linea
  # Lo empleamos para que PESCADO sea el unico valor en la linea, y no confundir con otros productos
  
  if (any(grepl(x=ListaProductos, pattern='^PESCADO$'))) {
    
    Pescado <- TRUE

    # Guardo la posicion del primer PESCADO
    PrimerPescado <- grep(x=ListaProductos, pattern='^PESCADO$')[1]
    
    final <- PrimerPescado-1
    
    Productos <- ListaProductos[0:final]
    
    # Eliminamos los otros PESCADO. Es decir, tomamos los opuestos
    inicio <- PrimerPescado+1
    
    ListaProductosPeso <- ListaProductos[inicio:length(ListaProductos)]
    
    indices <- !grepl(x=ListaProductosPeso, pattern='^PESCADO$')
    
    ProductosPeso <- ListaProductosPeso[indices] 
  
    }
  
  # Pescado = Valor bool para saber si tiene pescado
  # Productos = Todos los productos unitarios, o por peso, si no tiene PESCADO
  # ProductosPeso = Los productos, pescados y, frutas y verdutas
  
  if (Pescado){
    valores <- list(Pescado, Productos, ProductosPeso)
  } else {
    valores <- list(Pescado, ListaProductos)
  }
  
  return(valores)
}

```

Una vez que se identificara el tipo de producto, unitario o al peso, ya sería más fácil identificar la categoría a la que pertenece, por el formato en que fue registrado el producto en el ticket.

-   Patron producto unitario: Cantidad(1), descripción, importe
-   Patron producto unitario: Cantidad(+1), descripción, precio unitario, importe
-   Patron pescado: descripción, peso, precio unitario, importe
-   Patron frutas y verduras: Cantidad(1), descripción, peso, precio unitario, importe

```{r include=FALSE}

# Patrones para diferenciar a los productos y sus respectivos precios

# Sabemos que disponemos de 3 tipos de productos
TipoProducto <- c('Unitario', 'Pescado', 'Fruta y verdura')

# ^([0-9]+) --- Permite que el primer elemento sea un numero 
# Luego de un espacio, \\s , lee cualquier caracter, excepto salto de linea --- (.+?)
# Luego de otro espacio, \\s , lee el valor unitario --- ([0-9]+,[0-9]{2})
# Luego de otro espacio, \\s , lee el importe total, indicando el final de la linea --- ([0-9]+,[0-9]{2})$

# Formato normal, para productos unitarios con mas de una unidad
patron_normal <- "^([0-9]+)\\s(.+?)\\s([0-9]+,[0-9]{2})\\s+([0-9]+,[0-9]{2})$"

# Caso 2: Cuando cantidad es 1. Es decir, sin precio unitario
patron_cantidad_1 <- "^1\\s(.+?)\\s+([0-9]+,[0-9]{2})$"

# Caso 3: Productos por peso, tanto pescado como fruteria.

# ^[A-ZÁÉÍÓÚÑ]+ --- La primera parte del texto sea una palabra en mayúscula
# (?:\\s+[A-ZÁÉÍÓÚÑ]+)* --- Después de la primera palabra, pueden haber más; puede o no ocurrir
# \\s*(?:[0-9/]+)? --- Puede o no leer los numeros con las barras; ?? hace que sea opcional
# (?:\\s+[A-ZÁÉÍÓÚÑ]+)*$ --- Pueden haber más palabras en mayúsculas

patron_pescado <- "(?i)^[A-ZÁÉÍÓÚÑ]+(?:\\s+[A-ZÁÉÍÓÚÑ]+)*\\s*(?:[0-9/]+)?(?:\\s+[A-ZÁÉÍÓÚÑ]+)*$"

patron_peso <- "^([0-9]+)\\s(.+)$"

patron_precio_peso <- "^([0-9,]+)\\s*kg\\s*([0-9,]+)\\s*€/kg\\s*([0-9,]+)$"

```

```{r include=FALSE}

# Funcion que registra datos de los productos si tiene pescado.

# NOTA: Esta funcion solo sirve si se ha comprado pescado. Por eso se creo la funcion anterior para detectar si había # pescado o no en la compra

# InformacionCompra = lista recuperada de ExtraerInformacion
# Parking = valor bool recuperado de DetectaParking
# ListaProductosUnitarios = lista recuperada de la funcion DetectaPescado
# ListaProductosPeso = lista recuperada de la funcion DetectaPescado, siempre que se haya comprado pescado
# ProductosRegistrados = dataframe Compra, que contendrá todos los productos leidos

RegistraProductosConFiltro <- function (InformacionCompra, Parking, ListaProductosUnitarios, ListaProductosPeso, ProductosRegistrados){
  
  fecha <- InformacionCompra[[1]][[5]]
  hora <- InformacionCompra[[1]][[6]]
  idtienda <- InformacionCompra[[1]][[7]]
  idfactura <- InformacionCompra[[1]][[9]]
  
  # Tratamiento para los productos unitarios
  
  i <- 1
  
  while (i <= length(ListaProductosUnitarios)){
    
    p <- ListaProductosUnitarios[i]
    
    if (grepl(x=p, pattern=patron_cantidad_1)){
      
      detalleproducto <- str_match(string=p, pattern=patron_cantidad_1)[2:3]
      
      nombreproducto <- detalleproducto[1]
      precio <- detalleproducto[2]
      importe <- detalleproducto[2]
      tproducto <- TipoProducto[1]
      
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Hora = hora,
                    Producto = nombreproducto, 
                    Cantidad = '1', 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto, 
                    Parking = Parking,
                    stringsAsFactors = FALSE)
      
      ProductosRegistrados <- rbind(ProductosRegistrados, valores)
      
    } else {
      # Si no sigue el patron de una unidad, seguirá el patrón de más de una unidad
      
      detalleproducto <- str_match(string=p, pattern=patron_normal)[2:5]
      
      cantidad <- detalleproducto[1]
      nombreproducto <- detalleproducto[2]
      precio <- detalleproducto[3]
      importe <- detalleproducto[4]
      tproducto <- TipoProducto[1]
      
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Hora = hora, 
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto,
                    Parking = Parking,
                    stringsAsFactors = FALSE)
      
      ProductosRegistrados <- rbind(ProductosRegistrados, valores)
      
    }
    i <- i + 1
  }
  
  # Tratamiento para los datos por peso
  
  i <- 1
  
  while (i <= length(ListaProductosPeso)){
    
    p <- ListaProductosPeso[i]
    
    if (grepl(x=p, pattern=patron_pescado)){
      
      nombreproducto <- p
      
      i <- i + 1
      
      detalleproducto <- ListaProductosPeso[i]
      detalleproducto <- str_match(string=detalleproducto, pattern=patron_precio_peso)[2:4]
  
      cantidad <- detalleproducto[1]
      precio <- detalleproducto[2]
      importe <- detalleproducto[3]
      tproducto <- TipoProducto[2]
      
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Hora = hora,
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto,
                    Parking = Parking,
                    stringsAsFactors = FALSE)
      
      ProductosRegistrados <- rbind(ProductosRegistrados, valores)
      
    } else {
      
      # Si no sigue el patron de pescado, seguirá el patron de frutas y verduras; por peso. 
      
      nombreproductopeso <- str_match(string=p, pattern=patron_peso)[2:3]
      
      cantidad <- nombreproductopeso[1]
      nombreproducto <- nombreproductopeso[2]
      
      i <- i + 1
      
      detalleproducto <- ListaProductosPeso[i]
      detalleproducto <- str_match(string=detalleproducto, pattern=patron_precio_peso)[2:4]
  
      cantidad <- detalleproducto[1]
      precio <- detalleproducto[2]
      importe <- detalleproducto[3]
      tproducto <- TipoProducto[3]
      
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Hora = hora,
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto, 
                    Parking = Parking,
                    stringsAsFactors = FALSE)
      
      ProductosRegistrados <- rbind(ProductosRegistrados, valores)
    }
    i <- i + 1
  }
  
  # Solo devolverá el dataframe que contiene los productos registrados
  
  return(ProductosRegistrados)
}
```

```{r include=FALSE}

# Funcion que registra datos de los productos cuando no se ha comprado pescado

RegistraProductosSinFiltro <- function (InformacionCompra, Parking, ListaProductos, ProductosRegistrados){
  
  fecha <- InformacionCompra[[1]][[5]]
  hora <- InformacionCompra[[1]][[6]]
  idtienda <- InformacionCompra[[1]][[7]]
  idfactura <- InformacionCompra[[1]][[9]]
  
  # Tratamiento para los productos unitarios

  i <- 1
    
  while (i <= length(ListaProductos)) {
    
    p <- ListaProductos[i]
    
    if (grepl(x=p, pattern=patron_normal) | grepl(x=p, pattern=patron_cantidad_1)){
      
      tproducto <- TipoProducto[1]
      
      if (grepl(x=p, pattern=patron_normal)) {
        
        # Productos que tienen mas de una cantidad 
        
        detalleproducto <- str_match(string=p, pattern=patron_normal)[2:5]
        
        cantidad <- detalleproducto[1]
        nombreproducto <- detalleproducto[2]
        precio <- detalleproducto[3]
        importe <- detalleproducto[4]
        
        valores <- data.frame(
                      Fecha = fecha, 
                      IdTienda = idtienda, 
                      IdFactura = idfactura, 
                      Hora = hora, 
                      Producto = nombreproducto, 
                      Cantidad = cantidad, 
                      PrecioUnitario = precio, 
                      Importe = importe, 
                      TipoProducto = tproducto,
                      Parking = Parking,
                    stringsAsFactors = FALSE)
        
        ProductosRegistrados <- rbind(ProductosRegistrados, valores)
        
      } else if (grepl(x=p, pattern=patron_cantidad_1)){
        
        # Productos que tienen una (1) cantidad 
          
        cantidad <- '1'
        
        detalleproducto <- str_match(string=p, pattern=patron_cantidad_1)[2:3]
        
        nombreproducto <- detalleproducto[1]
        precio <- detalleproducto[2]
        
        valores <- data.frame(
                      Fecha = fecha, 
                      IdTienda = idtienda, 
                      IdFactura = idfactura, 
                      Hora = hora, 
                      Producto = nombreproducto, 
                      Cantidad = cantidad, 
                      PrecioUnitario = precio, 
                      Importe = precio, 
                      TipoProducto = tproducto,
                      Parking = Parking,
                    stringsAsFactors = FALSE)
        
        ProductosRegistrados <- rbind(ProductosRegistrados, valores)
      }
      
    } else {
      
      # Si no tienen el patron normal o de una cantidad, entonces es un producto por peso 
      
      # Tratamiento producto por peso, para frutas y verduras
      
      tproducto <- TipoProducto[3]
      
      nombreproductopeso <- str_match(string=p, pattern=patron_peso)[2:3]
      
      cantidad <- nombreproductopeso[1]
      nombreproducto <- nombreproductopeso[2]
      
      i <- i + 1
      
      detalleproducto <- ListaProductos[i]
      detalleproducto <- str_match(string=detalleproducto, pattern=patron_precio_peso)[2:4]
  
      cantidad <- detalleproducto[1]
      precio <- detalleproducto[2]
      importe <- detalleproducto[3]
      
      valores <- data.frame(
                    Fecha = fecha, 
                    IdTienda = idtienda, 
                    IdFactura = idfactura, 
                    Hora = hora, 
                    Producto = nombreproducto, 
                    Cantidad = cantidad, 
                    PrecioUnitario = precio, 
                    Importe = importe, 
                    TipoProducto = tproducto, 
                    Parking = Parking,
                    stringsAsFactors = FALSE)
      
      ProductosRegistrados <- rbind(ProductosRegistrados, valores)
    }
    i <- i + 1
  }
  
  # Solo devolverá el dataframe que contiene los productos registrados
  return(ProductosRegistrados)
  
}

```

```{r include=FALSE}

# General 

# Funcion que almacena:

# 1. El nombre de los ficheros que no fueron leidos.

# 2. El nombre de los ficheros que fueron leidos; los que corresponden a Mercadona. 

# 3. Información de todas las tiendas que fueron leidas.

# 4. Información sobre productos y compras realizadas


# Funcion para almacenar los ficheros leidos y los que no. 

# Le paso el dataframe de los ficheros leidos y no leidos para que los modifique, 
# si se añade un nuevo fichero. 

RegistroFicheros <- function(Nombrepdf, DF_FicherosNoLeidos, DF_FicherosLeidos, DF_Tiendas, DF_Compras) {
  
  Verifica <- EsMercadona(Nombrepdf)
  
  # Bool para saber si es de mercadona o no
  V <- Verifica[[1]]
  
  if (V) {
    
    # Solo si es de Mercadona 
    
    # Todas las lineas leidas
    Lineas <- Verifica[[2]]
    
    Informacion <- ExtraeInformacion(Lineas)
    
    # Todo el bloque que contiene los productos, incluido el Parking
    Productos <- Informacion[[2]]
    
    # FILTRO 1
    filtro1 <- DetectaParking(Productos)
    
    # Valor bool para saber si tiene parking o no
    ValorParking <- filtro1[[1]]
    
    # Todo el bloque que contiene los productos sin el parking
    Productos <- filtro1[[2]]
    
    # FILTRO 2
    filtro2 <- DetectaPescado(Productos)
    
    # Valor bool para saber si tiene pescado o no
    Pescado <- filtro2[[1]]
    
    direccion <- Informacion[[1]][[1]]
    codpos <- Informacion[[1]][[2]]
    municipio <- Informacion[[1]][[3]]
    tel <- Informacion[[1]][[4]]
    id_tienda <- Informacion[[1]][[7]]
    id_factura <- Informacion[[1]][[9]]
    
    # Añadimos los ficheros que fueron leidos correctamente y además, añadimos la factura a la que 
    # corresponde para verificar posteriormente
    
    if (!id_factura %in% DF_FicherosLeidos$FacturaFichero){
      
      valores <- data.frame( FacturaFichero = id_factura,
                             NombreFichero = Nombrepdf,
                             stringsAsFactors = FALSE )
      
      DF_FicherosLeidos <- rbind(DF_FicherosLeidos, valores)
      
    }
    
    if (!id_tienda %in% DF_Tiendas$IdTienda){
      
      valores <- data.frame( IdTienda = id_tienda,
                              Direccion = direccion,
                              CodigoPostal = codpos,
                              Municipio = municipio,
                              Telefono = tel,
                             stringsAsFactors = FALSE )
      
      DF_Tiendas <- rbind(DF_Tiendas, valores)
    }
    
    # INFORMACION SOBRE LOS PRODUCTOS COMPRADOS
    
    if (Pescado) {
      
      ProductosUnitarios <- filtro2[[2]]
      ProductosPeso <- filtro2[[3]]
      
      DF_Compras <- RegistraProductosConFiltro( InformacionCompra = Informacion,
                                 Parking = ValorParking,
                                 ListaProductosUnitarios = ProductosUnitarios,
                                 ListaProductosPeso = ProductosPeso,
                                 ProductosRegistrados = DF_Compras
                                 )
    } else {
      
      Productos <- filtro2[[2]]
      
      DF_Compras <- RegistraProductosSinFiltro ( InformacionCompra = Informacion,
                                             Parking = ValorParking,
                                             ListaProductos = Productos,
                                             ProductosRegistrados = DF_Compras)
    }

  } else {
    
    # Si no es de Mercadona
    # INFORMACION GENERAL DE LOS FICHEROS
    
    if (!(Nombrepdf %in% DF_FicherosNoLeidos$NombreFichero)) {
      
      valor <- data.frame(NombreFichero = Nombrepdf,
                           stringsAsFactors = FALSE )
    
      DF_FicherosNoLeidos <- rbind(DF_FicherosNoLeidos, valor)
    }
  }

  valores <- list(FicherosNoLeidos = DF_FicherosNoLeidos,
                  FicherosLeidos = DF_FicherosLeidos,
                  Tiendas = DF_Tiendas,
                  Compras = DF_Compras)
  
  # Devuelve todos los dataframes con la respectiva información
  
  return(valores)
}

```

```{r include=FALSE}

# Verificacion de las funciones anteriores para todos los ficheros

## Carga de todos los ficheros

#lista_ficheros <- list.files(path = 'data/', pattern = "\\.pdf$")
lista_nombres_ficheros <- list.files(path = 'data/')
length(lista_nombres_ficheros)
```

```{r include=FALSE}

## Bucle para cargar la informacion de cada fichero

for (f in lista_nombres_ficheros) {
  
  Resultado <- RegistroFicheros(Nombrepdf = f,
                                 DF_FicherosNoLeidos = NoLeido, 
                                 DF_FicherosLeidos = FicheroLeido, 
                                 DF_Tiendas = Tienda, 
                                 DF_Compras = Compra)
  
  NoLeido <- Resultado$FicherosNoLeidos
  FicheroLeido <- Resultado$FicherosLeidos
  Tienda <- Resultado$Tiendas
  Compra <- Resultado$Compras
  
}
```

## Ficheros no leidos

Solo aquellos ficheros que estuvieran con el formato correcto, serían tratados. Es decir, aquellos ficheros que tuvieran un escaner, foto u otro formato de factura distinto al formato de Mercadona, no serían tratados. Para identificarlos y corroborar su contenidos, hemos almacenado el nombre del fichero en un dataframe diferente.

```{r include=FALSE}

# Comprobacion de los ficheros no leidos 

lista_f_almacenados <- c(NoLeido$NombreFichero, FicheroLeido$NombreFichero)
diferencia <- setdiff(lista_nombres_ficheros, lista_f_almacenados)
diferencia

```

Adicional, para verificar la lectura de los datos, se filtró el dataframe que contiene toda la informacion de los productos comprados por los valores NA; si la carga se hizo correctamente, el valor de NA dentro del dataframe debería ser 0.

```{r include=FALSE}

# De estos nombres, identificamos que se trata de ficheros repetidos. Ya fueron leidos anteriormente 
 #y por tanto, no se vuelven a leer ni almacenar. 

## Verificamos los valores NA, ya que no deberian haber
sum(is.na(Compra))

```

## Presentación parcial de los datos

Esta es una representación sobre cómo se almacenaron los datos para empezar a trabajar sobre ellos. En la Tabla \@ref(tab:TablaTiendas) podemos ver todas las tiendas en las que se han hecho las compras. En total, se han registrado 35 tiendas.

```{r TablaTiendas, echo = FALSE, results ='asis'}

kable(head(Tienda, 10), caption = "Tabla de tiendas registradas")

```

En la siguiente Tabla \@ref(tab:TablaProductos) se muestran los productos que fueron leídos, identificando por ejemplo el ticket de compra, la fecha, el tipo de producto, entre otros.

```{r TablaProductos, echo = FALSE, results ='asis'}

kable(head(Compra, 15), caption = "Tabla de productos leídos")

```

# Limpieza posterior a la lectura

## Valores vacios

No debería haber muchos problemas con valores faltantes como ya hemos comprobado antes, pero vamos a hacer una segunda comprobación de ambos dataframes: Compra y Tienda

Compra presenta todas las instancias completas

```{r}
vis_miss(Compra)
```

Al igual que tienda

```{r}
vis_miss(Tienda)
```

## Problemas con tipos

# Resultados

# Conclusiones
